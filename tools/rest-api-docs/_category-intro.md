The new REST API replaces the old [HTTP Remote Control](../http-remote-control.md) with a proper API that exposes most functions that can be performed in the UI. Enable it in [Settings](../../3_config/settings.md#tcp).

The OpenAPI Schema is available at `[companion url]/api/openapi.json`.

Interactive docs can be accessed via `[companion url]/api/docs`.
