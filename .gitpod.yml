image:
  file: .gitpod.Dockerfile

ports:
  - port: 8888
    onOpen: open-preview

tasks:
  - init: |
      ./tools/update.sh
      ./tools/build_writefile.sh
    command: yarn dev
vscode:
  extensions:
    - dbaeumer.vscode-eslint
